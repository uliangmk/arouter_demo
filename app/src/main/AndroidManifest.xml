<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.blued.myunitydemo">

    <application
        android:name="com.blued.c_module_main.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.blued.c_module_main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.blued.b_module_one.OneActivity" />
        <activity android:name="com.blued.b_module_two.TwoActivity" />
        <activity android:name="com.blued.b_module_three.ThreeActivity" />
    </application>

</manifest>